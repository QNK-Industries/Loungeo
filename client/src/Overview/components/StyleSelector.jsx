import React from 'react';

// const GH_TOKEN = require('../../../tokens.js');

export default function StyleSelector({ item, styles, rating }) {
  // console.log(item)
  console.log(styles)
  // console.log(rating)
  return (
    <div>Hello World</div>
  );
}
